#work with pull request

#push branch from VS or command line

git push
git push --set-upstream origin feature1

#process PR in web interface

git branch -d feature1


git tag -a v1.0
git push origin v1.0
git push --tags

#show in web interface